name: test
on:
 push:
    branches: [main]
jobs:
    testing:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout
            uses: actions/checkout@v4
          - name: testing
            run: echo "helloe world"   


            
